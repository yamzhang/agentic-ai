# assets 目录说明

本目录只保留**可复用、非敏感、适合纳入仓库**的静态资源。

当前建议：
- `few_shot/`：LLM few-shot 示例

不建议纳入仓库的内容：
- 飞书会议原始数据
- 客户真实转录文本
- 运行时生成的中间文件
- 本地调试输出
- 带有真实配置或凭据的文件

约定：
- 原始输入样本若含真实客户信息，默认不要提交
- 运行结果统一放 `runtime/`，且默认不入库
- 如果后续需要保留演示样本，优先做匿名化 / 脱敏后再放入 `assets/`
